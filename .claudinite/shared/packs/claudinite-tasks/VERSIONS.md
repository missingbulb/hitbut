# Version history

Records for `packs/claudinite-tasks/pack.mjs`'s `version` field, one row per bump.

| Version | Date | What changed |
|---|---|---|
| 60902.2 | 2026-09-02 | Declarative task preconditions (#1578). A task declares `preconditions` — a conjunction of positively-named conditions over the collected signals, `||` inside a term, `none` as the empty precondition, inline `:` arguments — evaluated by `precondition-policy.mjs` beside the merge-policy engine, at the one point a verdict was ever formed. `precondition_signals` is DERIVED from the expression, so the collector union cannot disagree with the gate; the legacy `precondition` function stays accepted forever for a member's own local task files, and a declaration carries exactly one of the two. It fails LOUD — an unknown term, a malformed argument or an unreadable signal is `{ error }`, a failed run in the failure lane, never a silent decline. A task whose gate is its own ships `preconditions.mjs` beside its `task.mjs`. THE SILENCE GATE: no task runs on a silent repo unless its declaration says so, carried by the vocabulary rather than an operator — the delivery lanes stamp `Claudinite-Task: <pack>/<task>` on what they commit and merge, and the movement conditions read that trailer, so a task's own output can never wake its neighbours and a task added tomorrow is classified correctly on its first run. Repo shape leaves the trigger for `taskScheduler.disabledTasks`, read by the scheduler before it instantiates anything. |
| 60902.1 | 2026-09-02 | The retired `merged-pr`/`open-pr` outcome spellings are swept out of `test/sim/sim.mjs`'s unused `ceiling` fixture field — the acceptance door itself (`task-contract.mjs`'s `LEGACY_OUTCOMES`, `verify-outcome.mjs`, `deliver-pr.md`) and every test exercising it keep the retired spellings as their inputs, unchanged (#1470). |
| 60901.2 | 2026-09-01 | Two questions the `fleet` and `localPacks` signals stopped asking. DORMANCY: `readFleet` skips a member declaring `dormant: true`, beside its existing fork/archived/canon skips, so `growth-discover-packs` (weekly) and `growth-promote` (daily) no longer sweep repos that declared themselves out of the recurring work, nor pay their per-member probes; every other fleet reader already called `isDormant`. LOCAL-PACK PRESENCE: neither reader probes whether a repo has local packs any more — adoption seeds `.claudinite/local/packs/<repo>/` and the nightly deliberately never re-seeds or removes it, so the answer was yes everywhere. The member record drops `hasLocalPacks` (one contents read per member per collection), `localPacks` drops `present`, and the checkout probe behind it goes with them (#1562). |
| 60901.1 | 2026-09-01 | The `executor-vars-bag` rewrite is re-issued at a version above where five members landed. Their apply-stage PRs were merged before the stage delivered the withheld executor, which stamped them past 60831.6 — and `migrationApplies` is `want > have`, so the record fell out of range, stopped vendoring, and the staged copy was swept as a leftover, leaving no way back. The re-issued record's `appliesTo` reads the destination's own content, so it is inert on the eight that received the line normally (#1545). |
| 60831.9 | 2026-08-31 | Coded production validations (#1530): a verification whose artifact is a URL routes to the new agentless `verify-production` task, whose worker fetches declarative `Live-probe:`/`Verify-probe:` lines Action-side — where egress exists — and judges them in code: liveness failing re-arms the item, a verify probe failing against a live release reopens `Original-issue:`, all passing closes with the evidence read. The re-arm rides a new code-work protocol: a worker printing `claudinite-requeue: <instant> — <reason>` before a clean exit has the executor stamp the item's `Not-before` (machine half) and return it to blocked instead of closing done — the third answer an exit code could not give. Recorded first as 60831.8, a number #1516 had already published, so no member holding that version would have received any of it; the row carries the version that actually delivers it (#1482). |
| 60831.7 | 2026-08-31 | The janitor gains rule H, the unclosed terminal: an open item wearing `task:status:done` or `task:status:rejected` is a transition that tore — every writer closes in the same breath — so it reads as finished to every other rule and to the leash while sitting open in the queue forever. The close is the whole repair, at the status's own outcome, with no relabelling. Two guards against a converge in flight, an hour's clock and a fresh read before acting (#1526). |
| 60831.6 | 2026-08-31 | A migration record carries `CLAUDINITE_VARS: ${{ toJSON(vars) }}` into each member's LIVE executor workflow, so a member that adopted before #1494 stops holding the bag reader with no bag to read. A rewrite rather than a materialize, anchored above the `# claudinite:secrets` marker: the executor is not identical across members and a wholesale copy would take each one's stamped `required_secrets` with it. Needs `claudinite-lifecycle` 60831.1's withhold lane to deliver (#1518). |
| 60831.5 | 2026-08-31 | Janitor rule E never supersedes an AD-HOC park: every ad-hoc item runs the same task, so a later run of it is evidence about that run and not about this issue's own work. Two verification issues were closed on a third issue's evidence, their own `Verify:` assertion never executed (#1498). |
| 60831.4 | 2026-08-31 | The scheduler run's work-item list reads membership through `isQueueItem`, the shared predicate, rather than the `[claudinite-work]` title prefix — an adopted marked issue keeps the person's own title forever, so the prefix test dropped exactly that class from the one job that releases a blocked item, and ad-hoc items slept days past their `Not-before:` (#1497). The executing-leash reclaim reads such an item's task off its worker path too, so `implement-request`'s `on_interrupt: 'needs-human'` is honoured rather than silently re-queued. |
| 60831.3 | 2026-08-31 | The executor carries every repository VARIABLE as one static `CLAUDINITE_VARS: ${{ toJSON(vars) }}` line, so a task needing a repo variable no longer needs the workflow to name it — the human-merged-PR-per-value coupling that still binds secrets. `vars-bag.mjs` reads it and `taskEnv` unpacks it additively, never overwriting a name the runner's environment already carries; unlike the secrets bag there is no per-task selection, `vars` being non-sensitive by construction. `toJSON(secrets)` stays guarded out of both stubs (#1492). |
| 60831.2 | 2026-08-31 | `converge-item.mjs` is stated and built as what it is: the transition a work-item SESSION performs, planned in code. `instructions.md` step 6 is its only caller, so the REST executor beside it was unreachable — and reached first, making a session meet an error before the path written for it and report that it could not converge. That is what left finished runs to the janitor's 3h leash, which parks them `needs-human-decision`. The file now plans and prints, always on stdout and exit 0; the item is handed in with `--item-file` instead of a second run behind an environment variable. The Actions-side transition is untouched in `executor.mjs` (#1491). |
| 60831.1 | 2026-08-31 | A `task:status:done` terminal closes the issue it stands on, marked or filed — the converge, the executor's code-work close and janitor rule G's merged half alike. `done` is the one outcome with nothing left for anyone to act on; every other terminal still stands on the open issue. Converge's membership test also accepts an item's origin label, so an item adopted before the machine block existed converges in code instead of being refused into a by-hand transition (#1489). |
| 60830.9 | 2026-08-30 | `usage-fold` authorizes `under:.claudinite/local && generated-file-changes` — the tree its aggregate lands in — where `generated-file-changes` alone authorized any GENERATED file in the repo, including another task's delivery (#1479). |
| 60830.8 | 2026-08-30 | `executor.md` and `queue/instructions.md` address their commands from `<here>`, the directory each file sits in, instead of an `<engine>` root that has named nothing since the task surface left `engine/scheduler/`; `instructions.md` never defined the placeholder it used for the terminal transition at all (#1475). |
| 60830.7 | 2026-08-30 | A policy term may name a folder inline, `under:<dir>`, covering any change of any kind inside that directory and nothing outside it — so an ad-hoc request or a task confined to one tree scopes itself without a pack committing a named `merge-rules.json` rule. A list stays a union; `&&` inside a term is the intersection (`under:product-wiki && doc-changes`), every part having to cover the file and every part's whole-diff constraint having to hold. Whitespace around `&&` canonicalizes away, so the stamped trailer stays one token; an unusable path, a composite or a whole-policy word inside an `&&` term invalidates the policy, which authorizes nothing (#1473). |
| 60830.6 | 2026-08-30 | A park can state what would end it. The converge stamps `Ends-when: #<n> closed` on any park given a `--pr`, and janitor rule G closes the item when that target resolves — `done` if it merged, `rejected` if it was closed unmerged. An approval park held an open pull request that nothing ever revisited, which is why rule E excludes the kind; this is what answers it (#1468). |
| 60830.5 | 2026-08-30 | The auto-merge decision becomes a policy: `expected_outcome` collapses to `none`/`pr` (legacy spellings normalize at the door, advisory rename at author time) and a `pr` task declares `automerge` — `nothing`, `anything`, or a list of built-in diff classes `merge-policy.mjs` measures the actual diff against (a pack may extend them as `merge-rules.json` data; anything unresolved fails closed). Landing procedure lives only in `deliver-pr.md` — a task.md describes the changes to perform, never what happens to them. `usage-fold` stops maintaining the `merge=ours` `.gitattributes` line in-run: the pack seeds it at adoption (`seedOps`) and the advisory `generated-merge-driver` drives any repo whose own file predates that. A landing run stamps the `Claudinite-Automerge-Policy:` trailer and the new blocking `automerge-policy-scope` work check re-measures the verdict; the request lane's `Merge:` field now carries any policy expression, `if-narrow` resolving to the `narrow-diff` composite (#1459). A work item open across a pack rename is no longer stranded: only its title's id is canonicalized, so its stored worker path keeps naming the pre-rename directory forever. The executor now closes such an item obsolete instead of parking it for a human, and janitor rule F closes the ones already parked — a `failure` park holds its task's lane, so each one had also stopped that task being scheduled at all (#1461). Two changes claimed this number (#1482), so a member that converged between the two merges took only one of them; both are in every mount from the next version on. |
| 60830.4 | 2026-08-30 | A failed run parks `failure` whatever its worker's triage marker asked for: routing the park by the marker let a worker downgrade its own non-zero exit into a non-blocking lane, so the standing slot freed and the task re-filed daily against an unfixed cause (seven copies in one member, twenty-two in another). The verdict survives as the human-facing instruction in the comment. Janitor rules E and F close a park once a later run of the same task converges clean, or once its task is no longer carried at HEAD — the verdict #1446 gave the executor, applied to the parked set it never picks (#1452). |
| 60830.1 | 2026-08-30 | A task deleted from the checkout while a run is in flight converges its item as obsolete instead of failing it to a human: the executor probes the task directory before it runs anything, and `runCodeWork` names the missing directory rather than letting Node report a missing `/bin/sh` (#1444). |
| 60827.5 | 2026-08-27 | `commentOnly` and its checkable-extension set move to `engine/checks/helpers/code-scanning.mjs`, beside the `stripComments` they were already built on; narrow-diff imports and re-exports them, so the queue's merge verdict and tidy-repo's improve-comments gate answer "comments only" the same way (#1383). |
| 60827.4 | 2026-08-27 | The terminal transition is planned as ops and executed by whichever transport the run has: REST in Actions, and the session's own GitHub tools where a subprocess has no route to them — which is every session `invoke.mjs` fires. The park for a convergence that could not run at all is fixed at the failure park, the only one that holds the task's lane (#1374). |
| 60827.2 | 2026-08-27 | A converge writes only to the item it holds: releasing a dependent a close makes due is the scheduler run's own readiness job alone, never the closing side's. `readyDependents`/`releasedBy` retire from `readiness.mjs` with no caller left (#1373, reversing #892's §15.19). |
| 60827.1 | 2026-08-27 | The request-parameter labels are gone from the code: a request's model and merge authorization are body fields, so `claude-model:*`, `claude-automerge` and the `REQUEST_LABELS` ensure-set had no reader left (#1380). |
| 60824.9 | 2026-08-24 | The scheduler run's request half no longer throws on its first real request: `parseBlockedBy` was named in `main` and never imported, unreachable while the adoption list was always empty. The blocker resolution is an exported step, so a test can drive it (#1354). |
| 60824.8 | 2026-08-24 | The request lane reads again: each mark is asked for on its own rather than both in one conjunctive `labels` filter, which had selected issues wearing both spellings and so nothing at all since #1138. Neither listing lets a failed read pass as an empty one (#1354). |
| 60824.7 | 2026-08-24 | The `engine/scheduler/` legacy-path shims are gone; the tests' legacy wire literals move to one shared module, since they are protocol a decoder must keep understanding rather than pointers (#1328). |
| 60824.6 | 2026-08-24 | The retired frequency spellings are no longer normalized — `LEGACY_FREQUENCIES` is emptied, so a declaration naming one fails validation instead of resolving to `daily` (#1234). |
| 60824.5 | 2026-08-24 | The drain gate counts what the run itself readied alongside what the queue read returns, so an item created milliseconds earlier can no longer be missed and left unclaimed until the next cron fire (#1340). |
| 60824.4 | 2026-08-24 | The executor stub's secrets block describes what the converge stamps there and nothing else; the reason a serialised secrets context is not used lives in `converge-workflows.mjs`, where the decision is implemented (#1331). |
| 60824.3 | 2026-08-24 | The executor workflow names its secrets again, stamped by the converge at the `# claudinite:secrets` marker: `toJSON(secrets)` is the shape GitHub's malicious-workflow detection flags, and a flagged workflow parks every run behind a human approval (#1336, reversing #1301). |
| 60824.2 | 2026-08-24 | Scheduling wiring is the pack's own `converge-workflows.mjs`, scaffolded at adoption; a migration record declares the pack into every member that runs scheduled work (#1317). |
| 60824.1 | 2026-08-24 | The pack's first version: the task execution and scheduling surface moves out of `engine/scheduler/` into `packs/claudinite-tasks/`, with `task-janitor`, `usage-fold` and the two task-declaration checks relocating in beside it (#1317). |
| 60827.3 | 2026-08-27 | The write-side flip (#1119): every writer applies the canonical `task:status:*`/`task:origin:*` spellings, a park becomes one label, and the legacy spellings become explicit `LEGACY_*` literals — written never, read forever, and still ensured because open items wear them. |
| 60830.2 | 2026-08-30 | The executor workflow triggers on `task:status:waiting-for-executor` alone: no fielded engine writes the legacy ready spelling any more, so the trigger that carried members across the flip comes out (#1119). |
| 60830.3 | 2026-08-30 | `parseRequestFields` records where a marked issue's parameters are filed — one block on the body's first lines (#1456). |
| 60831.8 | 2026-08-31 | The janitor stops writing `needs-human-decision` altogether: rule B (a dead agent) and rule D (a torn label swap) both park `needs-human-failure`. Neither is a choice a person made — each is breakage the machine noticed — and `decision` is the one kind rule E can never supersede, so a later clean run, which is proof the earlier one is no longer broken, could not clear them and they accumulated. `failure` is also the blocking park, so a task's lane now holds until a person has looked instead of filing a fresh occurrence every anchor (#1515). |
