# Version history

Records for `packs/basics/pack.mjs`'s `version` field, one row per version, newest first.
A version is cut on `main` after its changes land, so a row names the pull requests that
landed between the previous version and this one; the weekly history task writes the rows
a version is missing and leaves every row that already stands.

| Version | Date | What changed |
|---|---|---|
| 60906.3 | 2026-09-06 | Keep Claudinite's own bookkeeping inside .claudinite/: mount attributes, no README row (#1754) |
| 60906.2 | 2026-09-06 | Declared checks at every moment: schema rung, work and action scopes, skill triggers, and the creation path (#1711) |
| 60906.1 | 2026-09-06 | Remove tests that only assert a declared value is set; widen the writing-tests rule (#1734) |
| 60905.2 | 2026-09-05 | Pack versions are cut on main by automation, never in the pull request (#1726) |
| 60905.1 | 2026-09-05 | Task declarations name what the run does to pull requests in the four-value `expected_outcome` vocabulary (#1695): `pr` became `fresh_pr` and `none` became `no_code_changes`, the same behaviour under the word that now sits beside `amend_existing_or_create_new_pr` and `supersede_existing_pr` — except that `ci-performance` declares `amend_existing_or_create_new_pr` (owner, 2026-09-04), its `task.md` delivering on the branch and pull request the item names. |
| 60904.3 | 2026-09-04 | Two rules become skills the guard forces for their files: `writing-repo-scanning-checks` (coded and declared checks) and `working-with-generated-files` (`**/*GENERATED*`) (#1662). |
| 60904.2 | 2026-09-04 | The directed folder-access graph is absorbed from the retired `barriers` pack (#1681): the config-driven `barrier` rule, the pack-contribution seam and the guide ([barriers.md](barriers.md)) live here, a repo's graph moves to this entry's `config.barriers`, and the mechanism no longer asks an adoption question nobody chose to answer. |
| 60904.1 | 2026-09-04 | Declarative checks: the four-moment design, the rule inventory, and pass two (derive → quantify) (#1676) |
| 60903.5 | 2026-09-03 | `writing-tests` and `authoring-agent-docs` force themselves for the files they are for (`force-load-on-file-edits-paths`, #1648): the guard holds an edit of a test file, a `CLAUDE.md` or a `.claude/rules/` file until the skill is loaded. |
| 60903.4 | 2026-09-03 | The legacy-tolerance rule and `writing-migration-plans` re-gate a tolerance's removal on a stated convergence window rather than on a condition that reads back true. A census — "when no holder still carries the old shape" — sounds like the rigorous choice and is usually unanswerable, so the removal waits on an answer that never arrives; the skill now says to read a count only where you own both sides of it, and the phase exit-condition example stops assuming another repo's state is observable from here (#1652). |
| 60903.3 | 2026-09-03 | A legacy tolerance is scaffolding, not a feature: `RULES.md` gains the rule that one ships with an advisory firing where the old shape is still in use and with its removal already a link in the migration's chain, and the migration bullet names that removal among the code written before approval. `writing-migration-plans` gains the section behind it — the three things that ship with a tolerance, the gate that reads back true rather than a date, making an unreadable gate the chain's first link, and the asymmetry that decides between a narrow policy and `nothing` — plus the review finding for a tolerance with neither advisory nor removal link (#1637). |
| 60903.2 | 2026-09-02 | `RULES.md` drops the descriptive framing the pack README already carries — the file carries rules only. |
| 60903.1 | 2026-09-03 | A skill's `SKILL.md` opens on what to do, not on what the skill is: the self-describing framing and the pointers to prose the reader already holds are gone. |
| 60902.6 | 2026-09-02 | Task declarations converted to `task.json`; the declaration's comments moved into each task's README (#1633). |
| 60902.5 | 2026-09-02 | `file-placement`'s pack-module pattern drops the retired `.claudinite/local_packs/` root: a repo's own packs are discovered from `.claudinite/local/packs/` and nowhere else (#1627). |
| 60902.4 | 2026-09-02 | Three rules on what human review is for: an automerge policy is a prediction of the change's folders and kinds, never widened to fit the diff; `nothing` for anything a later PR cannot undo; a diff wider than its ask is reported in the hand-over as a separation-of-concerns lesson. `writing-migration-plans` gains the sizing questions — how many PRs, each link's predicted diff and the policy it becomes, phases against divisions — and how the chain survives a policy park, a red CI and a PR closed unmerged (#1613). |
| 60902.3 | 2026-09-02 | `writing-handover-issues` gains the producer/consumer adjacency rule: a step that mints or reveals a value and the step that pastes it where it lives go immediately after each other, rather than a block of producers followed by a block of consumers. Every box between them is a value the reader holds in their head while doing something else, and one shown once is held there at the price of minting it again (#1592). |
| 60902.2 | 2026-09-02 | `ci-performance` converts to the declarative `preconditions`: `['substantive-change || prs-touched']`. It also narrows — any commit used to wake it, and another task's own delivery landing is the machinery running, not code whose CI timings could have moved (#1578). |
| 60902.1 | 2026-09-02 | The retired `merged-pr`/`open-pr` outcome spellings are swept out of the pack's own prose and fixtures that were not exercising the acceptance door: `writing-tests`' example assertion, and `ci-performance`'s task.md/task.mjs comments, which also correct their stale "always reviewed" landing description to the current `automerge: 'anything'` ceiling (#1470). |
| 60901.5 | 2026-09-01 | `references.md` gains the two checks #552 converted out of the `repo-text-sweeps` skill — `reference-integrity` and `markdown-link-labels` — with the sweep failure each was built to catch (#1576). |
| 60901.4 | 2026-09-01 | The pack adopts the references convention: `references.md` carries the incident evidence behind `verify-in-production` (#1121, #1460, the queue-park and label incidents), `do-later` (#1160), `writing-tests` (#429, #310, the hostname-apex case) and the `declared-check-spec-keys` check, and those prose files end the migrated rules with bare `(n)` markers instead of inline issue ids (#1564). |
| 60901.3 | 2026-09-01 | `authoring-agent-docs` narrows to the instruction files outside a pack: the "Shape a rule so it fires" house style moves to claudinite-growth's new `writing-pack-prose` skill, which also owns the per-pack `references.md` rationale convention, and a pointer says which skill to load when the file being written lives in a pack (#1560). |
| 60901.2 | 2026-09-01 | `declared-check-spec-keys` reads the engine's vocabulary through a namespace import and a capability guard: the pack lane delivers nightly and the engine lane only on a release, so this file spends a window beside an engine with no `unplacedSpecKeys` to export — and a named import of an absent export is a link-time error the loader records as a fault, which fails the self-test and parks the converge. On such an engine the rule is inert, which is the behaviour that engine already has (#1400). |
| 60901.1 | 2026-09-01 | `declared-check-spec-keys` — a declared check's key that the engine's vocabulary cannot place is now dropped at load and reported by this rule rather than thrown on, so a declaration written for a newer engine loads (with the unknown key inert) instead of dropping every rule in its file and wedging the member's converge. Advisory, because in a member the key is as likely to be skew as a typo; the canon overrides it to blocking, where engine and declarations ship from one commit (#1400). |
| 60831.6 | 2026-08-31 | `verify-in-production` gains the coded form (#1530): a URL-readable artifact files declarative `Live-probe:`/`Verify-probe:` lines routed to `claudinite-tasks/verify-production`, judged in code Action-side — no session, no egress wall, no `Model:`. The reachability gate becomes runner routing (URL → coded, GitHub read → agentic, neither → file nothing), and a fresh coded verification is forced to run at filing time and watched reporting **not yet live**, so the probes are proven failing before the release lands. |
| 60831.5 | 2026-08-31 | `writing-handover-issues` — an issue a PERSON executes is an instrument, not a description, so its body is a checklist and nothing else, with every explanation (what each step buys, what breaks while it is undone) below the last box rather than between them. Each box is one imperative line carrying the deepest URL that lands on the screen rather than a settings breadcrumb, and a step that produces a value names the control that mints it and where on that screen it appears. `verify-in-production`, `writing-migration-plans` and lifecycle's `adopt-pack` each re-spelled a fragment of that shape and now point here (#942). |
| 60831.4 | 2026-08-31 | The reachability gate is a filter rather than a router: an artifact the run cannot read is a reason to file nothing and say the change is unverified, and asking a person is a rare exception earned by a costly silent failure — not the fallback whenever no automatic check exists. A fleet-wide subject belongs to a routine holding fleet scope or to nobody. The same bar reaches every ad-hoc item in `RULES.md`: a marked issue asks an unattended session on this repo to do the work, and work it cannot do here parks minutes after being picked (#1520). |
| 60831.3 | 2026-08-31 | `production-retrospective` — an element that earned a design doc or a phased tracking issue schedules the review that comes back ~a week after it works as planned, asking working / misused / overused / underused against its production record. The brief is written at design time — expected amounts, behaviours, the signals for misuse, overuse and a wrong decision, the re-examinable decisions, and the metrics the run will read (built with the element) — never improvised at run time; findings route by kind, implementation faults to task issues and disproven decisions to discussion issues parked on the owner. Filed on the ad-hoc lane as a migration chain's last link or beside `verify-in-production` at the merge; the lane is open to further classes — canon-pack, local-pack or fleet (#1501). |
| 60831.2 | 2026-08-31 | `verify-in-production`'s playbook converges through `converge-item.mjs`, which needs no REST route and is the whole path on every session — and never a `task:status:*` label written by hand, which records a pass nowhere the queue reads and then hides the item from the leash (#1499). |
| 60831.1 | 2026-08-31 | `verify-in-production` gates on what the RUN can read before filing: a GitHub read in the routine's scope gets the automatic form, and a web page, deployed site, console or dashboard gets the human-step form — an ordinary assigned issue with no mark, rather than a queue item that spends a session rediscovering the wall and parks. The "watch it now" test asks whether the artifact becomes readable inside this session, not only whether it already did (#1500). |
| 60830.6 | 2026-08-30 | `do-later` proposes an `Automerge:` policy from the folder first — a deferral's tree is known at filing time, and a folder bound holds where a repo-wide kind class does not — reaching for a bare kind only where no single tree bounds the change, and for review where several unrelated trees do (#1483). |
| 60830.5 | 2026-08-30 | `runnable-doc-commands` — a `node <path>` written in a pack doc is a command an agent will run, and nothing opens it until one does; the check judges the two shapes the corpus owns (a placeholder-rooted path by its suffix, a mount path against the canon beneath it) and leaves a consuming repo's own paths alone (#1475). |
| 60830.4 | 2026-08-30 | `do-later`'s `Automerge:` vocabulary carries the inline folder scope `under:<dir>` and the `&&` intersection, so a deferral confined to one tree — or to that tree's documents — can authorize exactly that (#1473). |
| 60830.3 | 2026-08-30 | `/do-later` must settle the automerge value: the `Automerge:` field takes a policy expression, and a deferral that names none gets one `AskUserQuestion` proposing the narrowest fitting policy, review remaining the default only after the owner had the chance to choose. `ci-performance` declares `pr`/`anything` — CI itself gates a CI-performance fix (#1459). |
| 60830.2 | 2026-08-30 | The ad-hoc request skills place every field a run reads as one block on the issue body's first lines, and `verify-in-production`'s retry re-arms `Not-before:` to now + `Retry-every:` rather than to the stale value plus it (#1456). |
| 60830.1 | 2026-08-30 | The `ci-performance` brief names the park the engine writes — one `task:status:needs-human-<kind>` (#1395). |
| 60827.2 | 2026-08-27 | `do-later` can defer onto a *moment*: a time-worded ask ("check tomorrow") rides the queue's `Not-before:` wait field instead of falling through to "queues immediately" (#1393). |
| 60827.1 | 2026-08-27 | The task-janitor's queue sweep parks with ONE label — `task:status:needs-human-<kind>` — rather than the `needs-human` pair a half-applied swap could tear (#1119). |
| 60824.1 | 2026-08-24 | The `task-janitor` task moves to the new `claudinite-tasks` pack, which owns the queue it sweeps (#1317). |
| 60823.1 | 2026-08-23 | Prose and skills name the member settings file by its current name, `.claudinite-settings.json` (#1252). |
| 60822.1 | 2026-08-22 | The manifest stops restating its own tree (#1246): `id`, `prose`, `badge`, `skills`, `worldRules` and `workRules` are resolved from the pack directory and an absent `detect`/`marker` means no fingerprint. Coded rules move into `worldRules/`/`workRules/` and tests into `test/`, which no vendor set ships. `minEngineVersion` rises to the engine release that reads all of it. |
| 60821.4 | 2026-08-21 | Link PRs and issues where GitHub renders it (#1163) |
| 60821.3 | 2026-08-21 | The version bump belongs to the change, not to the release flow (#1151) |
| 60821.2 | 2026-08-21 | Migration plans run as a chain, not a checklist (#1156) |
| 60821.1 | 2026-08-21 | Label scheme PRs 1–2: decode every spelling, and the marked issue becomes the item (#1138) |
| 60820.5 | 2026-08-21 | The unified label scheme: design merged into tasks-dispatch, sim leads (#1120) |
| 60820.4 | 2026-08-21 | verify-in-production files only after the PR merges (#1131) |
| 60820.3 | 2026-08-20 | The comment classification stays a rule and stops being a check (#1118) |
| 60820.2 | 2026-08-20 | writing-migration-plans: a migration's cleanup is filed, not phased (#1107) |
| 60820.1 | 2026-08-20 | Engine and pack versions become date-anchored `<day>.<n>` (#1105) |
| 16 | 2026-08-20 | The tick becomes the scheduler run, and the janitor stops parking finished work (#1108) |
| 15 | 2026-08-20 | task.md describes only its own task; .claudinite/-only commits stop counting as project work (#1110) |
| 14 | 2026-08-20 | verify-in-production: the verification is the change's sub-issue (#1098) |
| 13 | 2026-08-20 | basics: a change proven only in production files its own verification (#1092) |
| 12 | 2026-08-20 | /do-later — defer a change into a chained, blocked request run (#1082) |
| 11 | 2026-08-20 | Pack reorganization: two collapses and two renames (#1081) |
| 10 | 2026-08-19 | Collapse chrome-extension-release into chrome-extension, and stop packs discussing each other (#1060) |
| 9 | 2026-08-19 | Give every pack file one of the four sanctioned shapes (#1056) |
| 8 | 2026-08-19 | Split needs-human into four triage sub-labels (#1051) |
| 7 | 2026-08-19 | Rename core → claudinite-lifecycle, grow_with_claudinite → claudinite-growth, and move the scheduled-task contract between them (#1029) |
| 6 | 2026-08-19 | Rename the code phase to code-work, and stop calling tasks scheduled-only (#1013) |
| 5 | 2026-08-18 | Task-owned trackers, and no fallback for a missing required input (#941) |
| 4 | 2026-08-18 | Retire the slot scheduler: delete run.mjs, the slot half of slots.mjs, the slot stub and FORCE_TASKS (#993) |
| 3 | 2026-08-18 | Pattern-check engine: self-describing spec keys, and the cross-file tree assertions (#800); resolve-dispatch: exit 0 for every verdict the routine goes on from (#817); Move the pack corpus onto the CLAUDE.md channel, off hook stdout (#812); Declared checks are JSON, one file per pack (#827); The apply stage lands its own delivery, instead of waiting a day for disposal (#832); A core pack: Claudinite's own surface, mandatory in every member (#836); Declarative checks: review document + the implementation it recommends (#839); core phase 2: the update task moves in, core-declared goes blocking (#844); Six declarative-vocabulary keys and the four conversions they unlock (#845); file-placement is advisory only — no acceptance entries (#858); The work-item queue: the whole mechanism, canon flipped (#854); Where CI time goes: fixture signing, the fleet-wide numbers, and a weekly measurement (#857); Flip the fleet to the work-item queue by changing the default (#888); Audit basics/RULES.md, and teach authoring-agent-docs what this rewrite needed (#894); The remaining conversion tranches: work scope, and files named by a parsed field (#908); Index every pack's rules and checks with words, severity and reason (#915); Promote eleven fleet lessons into the canon (#853); Add writing-migration-plans skill to basics (#932); Growth promote: 20 lessons from 8 members' local packs (#949); Move the environment setup script into the web pack, and converge the clone's git config at session start (#956); Bump every pack whose content was edited without a version bump (#969) |
| 2 | 2026-08-14 | Amend the rule-writing method from two independent rewrites (#775); Engine 2: local-pack declarations normalize themselves (#781); Claudinite growth: discover canon pack web-scraping (#739); The update runner, and the skew guard both mechanisms obey (#783); Phase 4: retire the last engine note and the held stamp (#785); Declarative pattern-check engine: one shared pass, checks declared as data (#790); Retire baselining, and restore what the update flows never took over (#791); prose-to-checks: the task declaration must agree with its own folder (#805); A pack file may not reference docs/ — one barriers rule, not a bespoke check (#806); Install runner CLI, and rename the mechanism to "versioned" (#809) |
| 1 | 2026-08-12 | No pack is active by default — the basics pack (né universal) is declared explicitly (#232); Package-driven skills: packs declare skills, mounts generated at session start (#234); Structure-review cleanups: co-locate pack tests, catalog drift-guard, doc/helper homes (#237); Rename "re-bootstrap" → "baselining"; name the baseline-migrations mechanism (#246); Fleet maintenance planner: code-decides / agents-act, pack-contributed run_daily tasks (#242); warning-suppression: pass suppressions that explain themselves on-site (#255); Pack dependencies at baselining; settings validation replaces pack-declaration (#244); Vendor chrome-extension-release plumbing out of core into the pack (#280); Decouple the migrations flow into its own standalone passes (#286); Dissolve growth/ into packs: canon-curation (home-only) + grow_with_claudinite; drop the growth barriers (#284); Make fleet daily maintenance MCP-native: no REST, census decoupled, migration repo unified (#294); Migrate the core⟂pack independence guard into the barriers pack, and decouple core from content (#283); Standardize tracking-issue titles; stop toggling their open/closed state (#298); Local packs: a project runs its own packs from .claudinite/local_packs/ (#300); tidy-repo: merge the 4 run_daily tasks into one, retire the ordering barrier (#308); Interactive-comment routing: three-mode prose + conversation-surface checks (#311); Vendored mount (phase 1): vendor writer, claudinite-isolation check, consumer CI stub (#320); Vendored mount (phase 1 complete): stamp-gated baselining refresh + fresh-path bootstrap (#321); Vendored mount (phase 2): the gated flip note — pilot on GoogleCalendarEventCreator (#324); Vendored-mount hardening: anti-rewind guards, resumable flip, same-day notes, pr-lane regeneration (#340); google-identity: prose → skill-owned checks, enforcement-silent canon docs, shared line-scanning lib (#350); grow_with_claudinite: conversation capture at merge, per-repo nightly extraction; lessons-learned skill retired (#352); Pack independence: no cross-pack code imports — compose by declaration + contributed config (#348); Local packs first-class: namespaced declarations; canon-curation moves to local_packs; local run_daily on by default (#361); Convert the fleet's direct-to-main commits to auto-merging PRs (#374); Rename maintenance.delivery values: push→auto, pr→review (#376); Vendored-mount surface shrink: engine/ consolidation, skills into packs, no CI stub, minimal CLAUDE.md + .gitignore (#384); Phase 3: retire the vendored-mount transition surface (#387); Scope-split conformance checks: fluent work context, independent world/work runners (#393); Per-project scheduling — Phase 0: engine/scheduler, groundwork, checks, task conversions (#396); Phase 1 canon groundwork: cron-minute hasher, cutover rescopes, contract renames (#399); Task pre-agent preprocessing: mechanism + §7 primitives (#411); agent-preprocessing E4 + E5: baselining converge-as-preprocessing, agentless common nights, drop canon from executor (#412); Unblock the E4 pilot: grant scheduler deliver() write perms + reconcile E5 docs (#416); Phase 2: per-repo scheduler tasks + canon cutover + self/fleet executor split (#421); required_secrets: the task spec, the wiring converge, and baselining (#423); Post-#421 fallout: barriers vs. migrations, the pinned migrationActive test, and the dispatch escape hatch (#424); Growth: promote the vendored-mount-currency rule from GCEC (#463); claudinite-isolation: the description promised a carve-out the rule dropped (#476); Phase 4 step 2: retire run_daily/ and delete the central fleet routine (#473); Baselining audit: six independent fixes to the run_daily → tasks ports (#494); Tighten every RULES.md to when + what + one non-obvious fact (#467); The outside look: a sheepdog freshness sweep, and CI test roots that cannot silently under-run (#496); Scheduler: an opaque override bag only the interested task reads (#513); Forcing a task must clear the SLOT gate too, not just the precondition (#516); file-placement: exempt the pack -> engine-surface reach the contract mandates (#517); Growth: promote 5 lessons from the fleet's local packs (#497); Drop vestigial agent_instructions from agent_model: 'none' tasks (#530); Pack badges: a mark per pack, and a README row bootstrap and baselining maintain (#525); Executor: cut executor.md to the instructions the executor acts on (#547); Growth promote: 10 lessons from 7 members into the canon (#541); prose-to-checks: add the deletion test and sweep the canon with it (#552); A project can declare itself dormant (#561); Commit-separation rule, and the three defects the deletion-test sweep surfaced (#560); Pack manifest as the single source: routing guidance, skills, scoped rules (#555); baselining: dispatch the repo's PR CI on the maintenance branch (#565) (#566); baselining: fail the run when the maintenance PR cannot be opened (#584); baselining: merge directly when the repo has no PR CI to gate on (#589); Claudinite growth: promote 5 lessons to canon (d2026-07-30) (#577); basics: name the path-filtered-conformance trap as a rule (#594); basics: bar pinning and requirement-shaped rules from packs (#604); scheduler: document the fleet executor routine and its scope word (#608); Prove a canon change is safe for consumers: self-test, rehearsal, canary, and the rule (#595); scheduler: let a task claim the run, and have baselining claim it when the mount is overdue (#623); static-website pack: date-anchored versions, release-on-push, explicit publish set (#611); baselining: dispose of the previous cycle's maintenance PR instead of inheriting it (#646); baselining: withhold workflow files from the push and let the agent land them (#651); migrations: gate workflow materialization on the caller, not the repo on disk (#656); scheduler: hand the agent its artifacts by identity, not by name (#657); Add comment rule: describe current state, not change history (#659); Move engineering-practices from a skill into basics/RULES.md (#661); Seed the README pack row at adoption only, not every baselining run (#662); baselining: carry the escalation reason to the agent, and stop discarding what the gates said (#667); baselining: land this cycle's PR when the arm could not — and the rule that says why (#669); Move the shipped-automation credential rule out of basics into the canon's own pack (#670); Three-responsibility task machinery: janitor split, precondition-only gating, prework rename, exec-status distillation (#675); basics: minimize task shelf-life — watch changes work now, never "check tomorrow" (#684); Delivery lands every cycle: fix the landing race, gate on base-branch requirements (#690); Run the converge's gates from the repo root, not the tree it just deleted (#691); bug-investigation: self-heal is a production fallback, not a bug-fix substitute (#693); One shared delivery procedure for every PR-delivering task (#698); Personal preferences as a pack, and one general primitive: a pack's own session-start step (#567); Migrations: one flat home, fetch-time relevance, no cleanup flows (#724); product-wiki: raise wiki-growth's ceiling to merged-pr, and stop restating it (#732); sheepdog: fleet-fit pack-recommendation sweep; retire session_scope (#707); Fleet fan-out model: the enforcer dispatches, the member executes (#750); growth-promote: dedupe PR #740's rule additions and cut the language (#751); Rewrite RULES.md as situation-keyed rules, and write down the method (#760); Versioned updates Phase 0: version scaffolding (#769) |
